{
    "_comment": "===== EXPERIMENT PARAMETERS =====",
    "_comment": "data should use one of the following: flowers, mnist, cifar10",
    "data": "MNISTcolored_2",
    "num_train_iters": 100000,
    "batch_size": 256,
    "seed": 123,
    "_comment": "===== IMAGE DATA PARAMETERS =====",
    "im_sz": 32,
    "im_ch": 3,
    "data_epsilon": 3e-2,
    "_comment": "===== NETWORK AND OPTIMIZER =====",
    "_comment": "net_type should use one of the following: vanilla, nonlocal",
    "net_type": "vanilla",
    "_comment": "optimizer_type should use one of the following: sgd, adam",
    "optimizer_type": "adam",
    "lr_init": 3e-5,
    "lr_min": 1e-5,
    "lr_decay": 0.99,
    "_comment": "===== LANGEVIN PARAMETERS =====",
    "epsilon": 1e-2,
    "num_shortrun_steps": 2000,
    "_comment": "shortrun_init should use one of the following: persistent, data, uniform, gaussian",
    "shortrun_init": "persistent",
    "pDOT_update_step": 10,
    "_comment": "===== DIAGNOSTIC PARAMTERS =====",
    "log_freq": 200,
    "log_longrun": true,
    "log_longrun_freq": 2000,
    "num_longrun_steps": 100000,
    "_comment": "longrun_init should use one of the following: persistent, data, uniform, gaussian",
    "longrun_init": "uniform",
    "_comment": "===== EOT PARAMETERS =====",
    "hreg": 0.1,
    "_comment": "cost should be 'l2sq'",
    "cost": "l2sq"
}
